<div class="container">
  <img src="https://m.media-amazon.com/images/I/616p7mG473L._SS500_.jpg" class="img-fluid" width="50" height="50">
  <a href="" download style="text-decoration: underline;">TreeOfLife.exe</a>
</div>

<h2 class="bg-primary text-white">Liste des abres</h2>
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th>Fichier</th>
      <th>Description</th>
      <th>Auteur</th>
      <th>Derniere modification</th>
    </tr>
  </thead>
  <tbody *ngFor="let binary of binaries">
    <td><a href="{{download(binary.path)}}">{{binary.path}}</a></td>
    <td>{{binary.description}}</td>
    <td>{{binary.author}}</td>
    <td>{{binary.lastmodified}}</td>
  </tbody>
</table>


<!----
<table>
  <tr>
    <th>Fichier</th>
    <th>Description</th>
    <th>Auteur</th>
    <th>Dernière modification</th>
  </tr>
  <tr *ngFor="let binary of binaries">
    <td><a href="{{download(binary.path)}}">{{binary.path}}</a></td>
    <td>{{binary.description}}</td>
    <td>{{binary.author}}</td>
    <td>{{binary.lastmodified}}</td>
  </tr>
</table>
-->